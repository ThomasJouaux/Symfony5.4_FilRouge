{% extends 'base.html.twig' %}


{% block body %}


		
			<div class="col-12 mb-3"></div>
			<div class='container-fluid'>
				<section class="row ">
					{% for sc in liste %}
						<div class="card col-3 ml-3 text-white bg-warning" style="">
							<div class="card-header">{{sc.SousCategorieNom}}</div>
							<div class="card-body">
								<a href="/produits/{{ sc.id }}"><img src="{{sc.img}}" width="250" height="500" class="img_catHomme"/></a>
								<p class="card-text">{{sc.SousCategorieType}}</p>
                                <a href="/produits/{{ sc.id }}" class="btn btn-warning">Voir les articles </a>
							</div>
						</div>

					{% endfor %}
				</section>
			</div>

		

</div>{% endblock %}
